<!doctype html>
<html lang="ja">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>送信完了｜セレネス</title>

<style>
:root{
  --bg:#fbf6f1;
  --card:#ffffff;
  --line:#eadfce;
  --text:#3b352e;
  --muted:#7a6f63;
  --acc:#c9a36a;
  --acc2:#b8945f;
  --shadow:0 14px 28px rgba(0,0,0,.08);
  --radius:28px;
}
*{box-sizing:border-box}
body{
  margin:0;
  font-family:-apple-system,BlinkMacSystemFont,"Noto Sans JP",sans-serif;
  background:
    radial-gradient(900px 520px at 20% 0%, rgba(201,163,106,.16), rgba(251,246,241,0) 55%),
    radial-gradient(900px 520px at 80% 10%, rgba(184,148,95,.12), rgba(251,246,241,0) 60%),
    var(--bg);
  color:var(--text);
}
.wrap{
  max-width:860px;
  margin:auto;
  padding:26px 18px 50px;
}

/* header */
.topbar{
  display:flex;align-items:center;justify-content:space-between;gap:12px;
  padding:8px 0 14px;
}
.brand{
  display:flex;align-items:center;gap:10px;text-decoration:none;color:inherit;
}
.logo{
  width:42px;height:42px;border-radius:16px;
  background:linear-gradient(180deg,var(--acc),var(--acc2));
  box-shadow:0 10px 18px rgba(201,163,106,.28);
  display:flex;align-items:center;justify-content:center;
  color:#fff;font-weight:1000;
}
.brand b{font-size:14px;letter-spacing:.04em}
.nav{
  display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end;
}
.nav a{
  text-decoration:none;color:inherit;
  padding:10px 12px;border-radius:18px;
  border:1px solid rgba(234,223,206,.9);
  background:rgba(255,255,255,.55);
  font-size:13px;
}
.nav a:hover{background:#fff}

/* card */
.card{
  background:rgba(255,255,255,.92);
  border:1px solid rgba(234,223,206,.9);
  border-radius:var(--radius);
  box-shadow:var(--shadow);
  overflow:hidden;
}
.hero{
  padding:22px;
  display:grid;
  grid-template-columns:1.2fr .8fr;
  gap:14px;
}
@media (max-width:820px){
  .hero{grid-template-columns:1fr}
}
.hTitle{
  margin:0 0 6px;
  font-size:20px;
  line-height:1.35;
  font-weight:1000;
}
.hLead{
  margin:0;
  color:var(--muted);
  line-height:1.95;
  font-size:13px;
}
.questStamp{
  display:inline-flex;align-items:center;gap:10px;
  margin:10px 0 0;
  padding:10px 14px;
  border-radius:999px;
  background:linear-gradient(180deg, rgba(201,163,106,.20), rgba(201,163,106,.10));
  border:1px solid rgba(201,163,106,.55);
  font-weight:1000;
  box-shadow:0 10px 18px rgba(0,0,0,.06);
}
.questStamp .icon{
  width:30px;height:30px;border-radius:999px;
  display:flex;align-items:center;justify-content:center;
  background:linear-gradient(180deg,var(--acc),var(--acc2));
  color:#fff;
}
.side{
  padding:18px;
  border-radius:22px;
  background:linear-gradient(180deg, rgba(201,163,106,.14), rgba(255,255,255,0));
  border:1px dashed rgba(201,163,106,.35);
}
.side b{display:block;margin:0 0 6px;font-size:13px}
.side p{margin:0;color:var(--muted);font-size:12px;line-height:1.9}

/* 今日の加護（神名） */
.blessing{
  margin-top:12px;
  padding:12px 14px;
  border-radius:18px;
  border:1px solid rgba(234,223,206,.9);
  background:rgba(255,255,255,.82);
}
.blessing .top{
  display:flex;align-items:center;gap:10px;flex-wrap:wrap;
  font-weight:1000;
}
.blessing .gem{
  width:28px;height:28px;border-radius:999px;
  display:inline-flex;align-items:center;justify-content:center;
  background:linear-gradient(180deg,var(--acc),var(--acc2));
  color:#fff;
  box-shadow:0 10px 18px rgba(0,0,0,.10);
  font-size:14px;
}
.blessing .name{
  font-size:14px;
}
.blessing .desc{
  margin:6px 0 0;
  color:var(--muted);
  font-size:12px;
  line-height:1.85;
}

/* next */
.nextBox{
  margin-top:12px;
  padding:14px;
  border-radius:22px;
  border:1px solid rgba(201,163,106,.45);
  background:linear-gradient(180deg, rgba(201,163,106,.12), rgba(255,255,255,.0));
}
.nextBox b{display:block;font-size:13px;margin-bottom:6px}
.nextBox p{margin:0;color:var(--muted);font-size:12px;line-height:1.9}
.nextRow{display:flex;gap:10px;flex-wrap:wrap;margin-top:10px}

/* actions */
.actions{
  display:flex;gap:10px;flex-wrap:wrap;
  margin-top:14px;
}
.btn{
  display:inline-flex;align-items:center;justify-content:center;gap:8px;
  padding:12px 16px;border-radius:22px;border:none;
  font-weight:1000;text-decoration:none;cursor:pointer;
}
.btn.primary{
  color:#fff;background:linear-gradient(180deg,var(--acc),var(--acc2));
}
.btn.ghost{
  background:#fff;border:1px solid var(--line);color:var(--text);
}
.small{
  margin-top:10px;
  font-size:12px;
  color:var(--muted);
  line-height:1.85;
}

/* timeline */
.section{
  padding:18px 22px 22px;
  border-top:1px solid rgba(234,223,206,.85);
}
.section h2{
  margin:0 0 10px;
  font-size:15px;
  letter-spacing:.03em;
}
.steps{
  display:grid;gap:10px;
  grid-template-columns:repeat(3,1fr);
}
@media (max-width:820px){.steps{grid-template-columns:1fr}}
.step{
  padding:14px;
  border-radius:22px;
  border:1px solid var(--line);
  background:rgba(255,255,255,.75);
}
.num{
  width:28px;height:28px;border-radius:999px;
  display:inline-flex;align-items:center;justify-content:center;
  font-weight:1000;color:#fff;
  background:linear-gradient(180deg,var(--acc),var(--acc2));
  box-shadow:0 10px 18px rgba(0,0,0,.10);
  margin-bottom:8px;
}
.step b{display:block;font-size:13px;margin-bottom:4px}
.step span{display:block;color:var(--muted);font-size:12px;line-height:1.8}

/* toast */
.toast{
  position:fixed;
  left:50%;
  bottom:18px;
  transform:translateX(-50%);
  padding:10px 12px;
  border-radius:999px;
  border:1px solid rgba(234,223,206,.9);
  background:rgba(255,255,255,.92);
  color:#7a6f63;
  font-size:12px;
  box-shadow:0 12px 24px rgba(0,0,0,.12);
  display:none;
}
</style>
</head>

<body>
  <div class="wrap">
    <div class="topbar">
      <a class="brand" href="index.html" aria-label="トップへ">
        <span class="logo" aria-hidden="true">🍺</span>
        <b>セレネス｜ルイーダの酒場</b>
      </a>
      <div class="nav">
        <a href="index.html">トップ</a>
        <a href="login.html">会員ページ</a>
        <a href="contact.html">お問い合わせ</a>
      </div>
    </div>

    <div class="card">
      <section class="hero">
        <div>
          <h1 class="hTitle">クエスト報告、受理しました</h1>

          <div class="questStamp">
            <span class="icon" aria-hidden="true">✅</span>
            <span>送信完了（受付済み）</span>
          </div>

          <p class="hLead" style="margin-top:10px">
            お問い合わせ内容は、確かにお預かりしました。<br>
            内容を確認のうえ、順次ご連絡いたします。
          </p>

          <!-- ① 今日の加護（神名） -->
          <div class="blessing" aria-label="今日の加護">
            <div class="top">
              <span class="gem" aria-hidden="true">🛡️</span>
              <span class="name">今日の加護：<span id="godName">—</span></span>
            </div>
            <p class="desc" id="godDesc">
              ※ これはゲーム演出です。結果や相性・成約などを保証するものではありません。
            </p>
          </div>

          <!-- ② 次におすすめ（価値観診断へ） -->
          <div class="nextBox" aria-label="次におすすめ">
            <b>次におすすめ：価値観診断で「軸」を整える</b>
            <p>
              価値観を入力すると、運営側で候補を検討しやすくなります。<br>
              迷ったら「一番近い」でOKです。
            </p>
            <div class="nextRow">
              <a class="btn primary" href="diagnosis.html">価値観診断へ進む</a>
              <a class="btn ghost" href="login.html">会員ページへ</a>
            </div>
            <div class="small" style="margin-top:8px">
              ※ 会員ページにログインしていない場合は、ログイン画面へ移動します。
            </div>
          </div>

          <div class="actions">
            <a class="btn ghost" href="index.html">街へ戻る（トップへ）</a>
            <a class="btn ghost" href="contact.html">追加で送る</a>
          </div>

          <div class="small">
            ※ ご返信までにお時間をいただく場合があります。<br>
            ※ 迷惑メール設定で返信が届かないことがあるため、受信設定のご確認をお願いします。
          </div>
        </div>

        <aside class="side">
          <b>酒場マスターからひとこと</b>
          <p>
            まずは落ち着いて、今の気持ちを整理しよう。<br>
            こちらで内容を確認し、無理のない形でご案内します。
          </p>
        </aside>
      </section>

      <section class="section">
        <h2>次の流れ（目安）</h2>
        <div class="steps">
          <div class="step">
            <div class="num">1</div>
            <b>内容確認</b>
            <span>受信した内容を確認し、必要に応じて確認事項を整理します。</span>
          </div>
          <div class="step">
            <div class="num">2</div>
            <b>ご連絡</b>
            <span>順次メール等でご連絡します（混雑時はお時間をいただきます）。</span>
          </div>
          <div class="step">
            <div class="num">3</div>
            <b>次の一歩</b>
            <span>ご希望に合わせて、案内・相談・手続きへ進みます。</span>
          </div>
        </div>

        <div class="small">
          ※ ご紹介に関する内容の場合でも、双方の合意が得られた場合のみ次のステップへ進みます。
        </div>
      </section>
    </div>
  </div>

  <div class="toast" id="toast">✅ 受付完了！この画面は保存不要です</div>

<script>
/* ちょい演出：下部トースト */
(function(){
  const t = document.getElementById("toast");
  if(!t) return;
  t.style.display="block";
  setTimeout(()=>{ t.style.display="none"; }, 2600);
})();

/* 今日の加護（神名）：日付で“毎日同じ”になる疑似ランダム */
(function(){
  const GODS = [
    { name:"アテナ",  desc:"知恵と戦略の加護。今日は「落ち着いて考える」が強みになりやすい日。" },
    { name:"アポロン",desc:"光と調和の加護。言葉を整えるほど、誤解がほどけやすい日。" },
    { name:"アルテミス",desc:"守護と自立の加護。無理をしない選択が、結果的に良い流れを作りやすい日。" },
    { name:"ヘルメス",desc:"連絡と橋渡しの加護。小さな一歩が次の展開につながりやすい日。" },
    { name:"ヘスティア",desc:"安心と家庭の加護。居心地の良さを大切にすると整いやすい日。" },
    { name:"ヘラ",    desc:"誠実と約束の加護。丁寧に進めるほど、信頼が積み上がりやすい日。" },
    { name:"ポセイドン",desc:"切り替えの加護。気持ちをリセットすると視界が開けやすい日。" },
    { name:"デメテル",desc:"育みの加護。焦らず育てる姿勢が、いちばんの近道になりやすい日。" },
    { name:"アフロディーテ",desc:"魅力とやさしさの加護。自分を大切に扱うほど、表情が柔らかくなりやすい日。" },
    { name:"ゼウス",  desc:"決断の加護。迷ったら「小さく決める」ことで前に進みやすい日。" },
    { name:"ハデス",  desc:"再出発の加護。過去を整理して、今の自分を選び直しやすい日。" },
    { name:"ヘファイストス",desc:"積み上げの加護。地道な準備が強みになりやすい日。" }
  ];

  function hashStringToInt(str){
    // 安定した簡易ハッシュ
    let h = 2166136261;
    for(let i=0;i<str.length;i++){
      h ^= str.charCodeAt(i);
      h = Math.imul(h, 16777619);
    }
    return (h >>> 0);
  }

  // “今日”で固定（端末のローカル日付）
  const now = new Date();
  const key = `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,"0")}-${String(now.getDate()).padStart(2,"0")}`;

  const idx = hashStringToInt(key) % GODS.length;
  const g = GODS[idx];

  const nameEl = document.getElementById("godName");
  const descEl = document.getElementById("godDesc");
  if(nameEl) nameEl.textContent = g.name;

  if(descEl){
    descEl.textContent =
      `${g.desc}\n※ これはゲーム演出です。結果や相性・成約などを保証するものではありません。`;
  }
})();
</script>

</body>
</html>